name: "Setup"
description: "Install pnpm, Node.js, and project dependencies"

inputs:
  frozen-lockfile:
    description: "Use --frozen-lockfile for pnpm install"
    required: false
    default: "true"
  token:
    description: "GitHub token for installing private git dependencies"
    required: false
    default: ${{ github.token }}

runs:
  using: "composite"
  steps:
    - uses: pnpm/action-setup@v5
      with:
        version: 10
    - uses: actions/setup-node@v6
      with:
        node-version: 24.x
        cache: "pnpm"
    - run: git config --global url."https://x-access-token:${{ inputs.token }}@github.com/".insteadOf "git@github.com:"
      shell: bash
    - run: pnpm install ${{ inputs.frozen-lockfile == 'true' && '--frozen-lockfile' || '--no-frozen-lockfile' }}
      shell: bash
