#!/bin/sh
# Skip in git worktrees — .git is a file there (not a directory).
# Agent worktrees get claude/hooks/pre-commit via core.hooksPath instead.
if [ -f ".git" ]; then
  exit 0
fi
pnpm exec lint-staged && pnpm run env:validate
